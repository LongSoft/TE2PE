CMAKE_MINIMUM_REQUIRED(VERSION 3.13)

PROJECT(TE2PE)

SET(PROJECT_SOURCES TE2PE.c)

ADD_EXECUTABLE(TE2PE ${PROJECT_SOURCES})

IF(UNIX)
 SET_TARGET_PROPERTIES(TE2PE PROPERTIES OUTPUT_NAME te2pe)
ENDIF()

INSTALL(
 TARGETS TE2PE
 RUNTIME DESTINATION bin
)
